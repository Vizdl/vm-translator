这是一个VM翻译器,主要功能就是将VM语言(虚拟机中间语言)翻译成HACK机器语言

运行方法 :
    案例测试 :
        在当前目录下输入命令 make,即可查看 ./tests 内测试案例的通过结果。
    自动输入 :
        cat tests/00_xcall.Pxml   | ./translator | cat
    输入该命令即可抓取 tests/00_xcall.Pxml(待翻译程序) 文件,通过管道输入翻译出结果,然后将HACK汇编输入到屏幕上。

功能支持 : 
    该翻译器支持jack语言编译后的基本语法,语法定义请看本人github上另一个项目 jack-compiler 项目的输出。
    或通过 ./test/ 文件中 *.Pxml 测试文件查看。
